{% for node in pages_list %}
    {% if group == node.group %}
        {% if page.url == node.url %}
            <li><a href="{{node.url}}" class="active">{{node.nav_title}}</a></li>
        {% else %}
            <li><a href="{{node.url}}">{{node.nav_title}}</a></li>
        {% endif %}
    {% endif %}
{% endfor %}
{% assign pages_list = nil %}
{% assign group = nil %} 
